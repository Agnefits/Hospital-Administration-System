

<aside class="sidebar" id="sidebar">
    <h4><i class="fas fa-hospital me-2"></i>Admin Panel</h4>

    @* @if (User.Identity.IsAuthenticated) *@
    @* { *@
    @*     var user = await UserManager.GetUserAsync(User); *@
    @*     <div class="mb-4"> *@
    @*         <small>Hello,</small><br /> *@
    @*         @* <strong>@user.UserName</strong> *@ 
    @*     </div> *@
    @* } *@

    <!-- Sidebar Navigation -->
    <aside class="sidebar" id="sidebar">
        <h4><i class="fas fa-hospital me-2"></i>Admin Panel</h4>
        <a href="#" class="nav-link"><i class="fas fa-users me-2"></i> Users</a>
        @* <a href="#" class="nav-link"><i class="fas fa-user-md me-2"></i> Doctors</a> *@
        @* <a href="#" class="nav-link"><i class="fas fa-procedures me-2"></i> Patients</a> *@
        <a href="#" class="nav-link"><i class="fas fa-calendar-check me-2"></i> Appointments</a>
        <a href="#" class="nav-link"><i class="fas fa-file-invoice-dollar me-2"></i> Billing</a>
        <a href="#" class="nav-link"><i class="fas fa-clipboard-list me-2"></i> Logs</a>
        @* <a href="#" class="nav-link"><i class="fas fa-chart-bar me-2"></i> Reports</a> *@
    </aside>

    <form asp-controller="Account" asp-action="Logout" method="post" class="mt-4">
        <button class="btn btn-danger w-100" type="submit">
            <i class="fas fa-sign-out-alt me-2"></i> Logout
        </button>
    </form>
</aside>
